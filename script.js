// Função chamada quando o usuário clica no botão, usa a funcionalidade pra criar putro texto
function alterarTexto1() {

    // Localiza o elemento com id="texto"
    let paragrafo = document.getElementById("texto");

    // Altera o conteúdo do parágrafo
    paragrafo.innerHTML = "foi eu que alterei este texto!";
}
function alterarTexto2() {

    // Localiza o elemento com id="texto"
    let paragrafo = document.getElementById("texto");

    // Altera o conteúdo do parágrafo
    paragrafo.innerHTML = "nao vale nada nem le!";
}